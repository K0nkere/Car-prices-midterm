service: "service.py:svc"  # Same as the argument passed to `bentoml serve`
labels:
    owner: kkr-ml-bookcamp
    stage: dev
include:
- "*.py"  # A pattern for matching which files to include in the bento
python:
    requirements_txt: "./requirements.txt"
  